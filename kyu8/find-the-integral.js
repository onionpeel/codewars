// const integrate = (c,e) =>  {
//   let newE = e + 1;
//   let value = c / newE;
//   return `${value}x^${newE}`;
// };

// const integrate = (c,e) => `${c / (e + 1)}x^${e + 1}`;

const integrate = (c,e) => `${c / e += 1}x^${e}`;
